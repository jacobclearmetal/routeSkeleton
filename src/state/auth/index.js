/**
 * This file would contain the reducer and selectors
 *
 * The dream is to have composable redux stores that follow the fractal component pattern
 */

// ALTERNATIVELY

/**
 * This file would contain the reducer, actions, and selectors (we already do this) as follows:
 * ------------------------------------------------------------------------
 *
 * export const types/constants = {
 *  ...
 * }
 *
 * export const initialState = {
 *  ...
 * }
 *
 * export default (state = initialState, action) = {
 *  ...
 * }
 *
 *
 * export const actions = {
 *  ...
 * }
 *
 * export getSomeSelectorFromState
 * export getSomeSelectorFromState
 * export getSomeSelectorFromState
 *
 */
